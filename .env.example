# SurvivalRAG Configuration
# Copy to .env and uncomment to override defaults
# Reference: docker-compose.yml

# LLM model for response generation (default: llama3.1:8b)
# Must be available in your Ollama instance
# SURVIVALRAG_MODEL=llama3.1:8b

# Ollama server URL (default: http://ollama:11434 in Docker, http://localhost:11434 natively)
# Set to point to an external Ollama instance (e.g., GPU machine on LAN)
# OLLAMA_HOST=http://192.168.1.100:11434

# Maximum chunks to retrieve per query (default: 5)
# SURVIVALRAG_MAX_CHUNKS=5

# Cosine similarity threshold for relevance filtering (default: 0.25)
# Lower values return more results; higher values are stricter
# SURVIVALRAG_RELEVANCE_THRESHOLD=0.25

# Cross-encoder reranker model (default: BAAI/bge-reranker-v2-m3)
# Improves retrieval precision 15-40% by re-scoring query+document pairs together
# Requires: pip install sentence-transformers
# Set to empty string or "none" to disable
# SURVIVALRAG_RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# --- Meshtastic Mesh Radio Bridge (optional) ---
# Requires: pip install meshtastic>=2.7.0
# Requires: Meshtastic radio connected via USB or WiFi

# Enable the Meshtastic bridge (default: false)
# MESHTASTIC_ENABLED=true

# Connection method: "serial" (auto-detect USB), "serial:/dev/ttyACM0", or "tcp://host:port"
# MESHTASTIC_CONNECTION=serial

# Channel index to listen/send on (0=primary, 1-7=secondary)
# MESHTASTIC_CHANNEL=0

# Message prefix that triggers queries (empty = respond to all messages)
# MESHTASTIC_TRIGGER=?

# Max bytes per message part, excluding [i/n] suffix (default: 194)
# MESHTASTIC_CHUNK_SIZE=194

# Maximum message parts per response (default: 5, range: 3-7)
# MESHTASTIC_MAX_PARTS=5

# Seconds between sending message parts (default: 2.0, use 5-10 for busy channels)
# MESHTASTIC_CHUNK_DELAY=2.0

# RAG response mode for Meshtastic responses (default: compact)
# MESHTASTIC_RESPONSE_MODE=compact

# Prefix on outgoing messages to prevent bot-to-bot loops
# MESHTASTIC_BOT_PREFIX=[RAG]
