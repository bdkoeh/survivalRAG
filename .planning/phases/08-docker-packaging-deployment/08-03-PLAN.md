---
phase: 08-docker-packaging-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DEPL-08

must_haves:
  truths:
    - "README.md contains a Quick Start section with the exact docker compose up command to start the system"
    - "README.md documents minimum hardware requirements: 16GB RAM, 20GB disk"
    - "README.md lists expected image sizes for both containers (app ~1GB, ollama ~7GB)"
    - "README.md documents GPU acceleration setup for Linux NVIDIA and the macOS limitation"
    - "README.md documents environment variable configuration with reference to .env.example"
    - "README.md documents how to use an external Ollama instance via OLLAMA_HOST"
    - "README.md documents CLI access via docker exec"
    - "README.md documents multi-arch builds (AMD64 + ARM64) using docker buildx"
  artifacts:
    - path: "README.md"
      provides: "Complete deployment documentation with quick start, hardware requirements, configuration, GPU setup, and troubleshooting"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Documents the docker compose up command and compose file usage"
      pattern: "docker compose"
    - from: "README.md"
      to: ".env.example"
      via: "References .env.example for configuration documentation"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "docker-compose.gpu.yml"
      via: "Documents GPU override compose file usage"
      pattern: "docker-compose\\.gpu\\.yml"
---

<objective>
Update README.md with comprehensive Docker deployment documentation: quick start guide, hardware requirements, environment variable configuration, GPU acceleration, CLI access, and troubleshooting.

Purpose: Non-technical users need clear documentation to deploy SurvivalRAG with a single command. The README is the first thing users see -- it must provide a zero-ambiguity path from `git clone` to a working system. DEPL-08 specifically requires documenting minimum hardware requirements (16GB RAM, 20GB disk).

Output: Updated README.md with deployment sections added
</objective>

<execution_context>
@/Users/pringle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pringle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-docker-packaging-deployment/08-CONTEXT.md
@.planning/phases/08-docker-packaging-deployment/08-RESEARCH.md
@.planning/phases/08-docker-packaging-deployment/08-01-SUMMARY.md
@.planning/phases/08-docker-packaging-deployment/08-02-SUMMARY.md

<interfaces>
<!-- Deployment details needed for README documentation. From CONTEXT.md and RESEARCH.md. -->

Docker commands:
- Quick start: `docker compose up` (builds on first run)
- Rebuild: `docker compose build`
- GPU mode: `docker compose -f docker-compose.yml -f docker-compose.gpu.yml up`
- CLI access: `docker exec -it survivalrag-app python cli.py ask "query"`
- CLI REPL: `docker exec -it survivalrag-app python cli.py`
- Stop: `docker compose down`
- Logs: `docker compose logs -f`

Environment variables (from .env.example):
- SURVIVALRAG_MODEL (default: llama3.1:8b)
- OLLAMA_HOST (default: http://ollama:11434 in Docker)
- SURVIVALRAG_MAX_CHUNKS (default: 5)
- SURVIVALRAG_RELEVANCE_THRESHOLD (default: 0.25)

Hardware requirements (from RESEARCH.md):
- Minimum: 16GB RAM, 20GB free disk
- Ollama image: ~7GB (base ~1GB + llama3.1:8b 4.9GB + nomic-embed-text 274MB)
- App image: ~1GB (python:3.14-slim 41MB + deps + ChromaDB 175MB + PDFs 314MB)
- Total: ~8-10GB for Docker images

Access points:
- Web UI: http://localhost:8080
- Ollama API: http://localhost:11434
- Health check: http://localhost:8080/api/health

GPU notes:
- Linux + NVIDIA: docker-compose.gpu.yml override
- macOS: Docker cannot access Metal GPUs; run Ollama natively for GPU acceleration
- NVIDIA Container Toolkit required for GPU passthrough
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Docker deployment documentation to README.md</name>
  <files>README.md</files>
  <action>
Read the existing README.md first to understand its current structure and content.

Add a comprehensive deployment section to README.md. Insert the deployment content as a major section. The exact placement depends on the existing structure, but it should be prominent (near the top, after any project overview).

**Add the following sections to README.md:**

**1. Quick Start section:**
```markdown
## Quick Start

### Prerequisites

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (v4.0+) or Docker Engine with Docker Compose v2
- 16GB RAM minimum
- 20GB free disk space

### Launch

```bash
git clone <repo-url>
cd survivalRAG
docker compose up
```

First build takes 10-20 minutes (downloads base images and bakes LLM models).
Once ready, open **http://localhost:8080** in your browser.

> The system is fully offline after the initial build -- no internet required at runtime.
```

**2. Hardware Requirements section:**
```markdown
## Hardware Requirements

| Resource | Minimum | Recommended |
|----------|---------|-------------|
| RAM | 16GB | 32GB |
| Disk | 20GB free | 30GB free |
| CPU | 4 cores | 8+ cores |
| GPU | Not required | NVIDIA GPU (Linux only) |

### Image Sizes

| Container | Size |
|-----------|------|
| survivalrag-app | ~1GB |
| survivalrag-ollama | ~7GB |
| **Total** | **~8GB** |

The Ollama image is large because it includes the bundled LLM models:
- **llama3.1:8b** (4.9GB) -- response generation
- **nomic-embed-text** (274MB) -- query embedding
```

**3. Configuration section:**
```markdown
## Configuration

Copy `.env.example` to `.env` and uncomment variables to override defaults:

```bash
cp .env.example .env
```

| Variable | Default | Description |
|----------|---------|-------------|
| `SURVIVALRAG_MODEL` | `llama3.1:8b` | LLM model for response generation |
| `OLLAMA_HOST` | `http://ollama:11434` | Ollama server URL |
| `SURVIVALRAG_MAX_CHUNKS` | `5` | Maximum chunks retrieved per query |
| `SURVIVALRAG_RELEVANCE_THRESHOLD` | `0.25` | Cosine similarity threshold |

### Using an External Ollama Instance

To use a GPU-equipped machine on your network instead of the bundled Ollama container:

1. Run Ollama on the GPU machine: `ollama serve`
2. Ensure the required models are available: `ollama pull llama3.1:8b && ollama pull nomic-embed-text`
3. Set `OLLAMA_HOST` in your `.env` file:
   ```
   OLLAMA_HOST=http://192.168.1.100:11434
   ```
4. Start only the app container: `docker compose up app`

### Using a Different Model

Set `SURVIVALRAG_MODEL` in your `.env` file. The model must be available in your Ollama instance:

```
SURVIVALRAG_MODEL=mistral:7b
```
```

**4. GPU Acceleration section:**
```markdown
## GPU Acceleration

### Linux (NVIDIA)

1. Install the [NVIDIA Container Toolkit](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html)
2. Start with the GPU override:
   ```bash
   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
   ```

### macOS (Apple Silicon)

Docker on macOS cannot access Apple Metal GPUs. For GPU acceleration on Mac:

1. Install Ollama natively: `brew install ollama`
2. Pull the required models: `ollama pull llama3.1:8b && ollama pull nomic-embed-text`
3. Start Ollama: `ollama serve`
4. Set `OLLAMA_HOST` in your `.env` file:
   ```
   OLLAMA_HOST=http://host.docker.internal:11434
   ```
5. Start only the app container: `docker compose up app`
```

**5. Multi-Architecture Builds section:**
```markdown
## Building for Multiple Architectures

The default `docker compose build` builds images for your host architecture only.
To build images for both AMD64 and ARM64 (e.g., for distribution or M-series Macs + x86 servers):

```bash
# Create a multi-arch builder (one-time setup)
docker buildx create --name survivalrag-builder --use

# Build app image for both architectures
docker buildx build --platform linux/amd64,linux/arm64 -t survivalrag-app -f Dockerfile .

# Build Ollama image for both architectures
docker buildx build --platform linux/amd64,linux/arm64 -t survivalrag-ollama -f Dockerfile.ollama .
```

> **Note:** Multi-arch builds for the Ollama image are slow because models must be pulled during build for each architecture. The base images (`python:3.14-slim` and `ollama/ollama`) already support both AMD64 and ARM64.
```

**6. CLI Access section:**
```markdown
## CLI Access

The CLI is available inside the app container via `docker exec`:

```bash
# Single query
docker exec -it survivalrag-app python cli.py ask "how to purify water"

# With category filter
docker exec -it survivalrag-app python cli.py ask --category medical "how to treat a burn"

# Interactive REPL
docker exec -it survivalrag-app python cli.py
```
```

**7. Troubleshooting section:**
```markdown
## Troubleshooting

| Problem | Solution |
|---------|----------|
| Build fails during model download | Ensure stable internet during `docker compose build` |
| "Ollama not available" in logs | Wait for Ollama health check to pass (up to 5 minutes on first start) |
| Very slow responses | Expected on CPU-only; see GPU Acceleration section |
| Port 8080 already in use | Change port mapping in docker-compose.yml: `"9090:8080"` |
| Out of disk space during build | Need 20GB+ free; clear Docker cache: `docker system prune` |

### Useful Commands

```bash
# Check container status and health
docker compose ps

# View logs
docker compose logs -f

# Rebuild after changes
docker compose build

# Stop and remove containers
docker compose down

# Check health endpoint
curl http://localhost:8080/api/health
```
```

**Important notes:**
- Preserve ALL existing README.md content -- add to it, do not replace
- Place the Quick Start section near the top of the file (after any existing overview/description)
- Use the existing README.md heading style and formatting conventions
- Do NOT add emoji to the README (per CLAUDE.md guidelines)
- Hardware requirements of "16GB RAM, 20GB disk" are per DEPL-08 and CONTEXT.md
  </action>
  <verify>
    <automated>python3 -c "
readme = open('README.md').read()

# Check required sections exist
assert 'Quick Start' in readme, 'Missing Quick Start section'
assert 'Hardware Requirements' in readme or 'Hardware' in readme, 'Missing Hardware Requirements section'
assert '16GB' in readme, 'Missing 16GB RAM requirement'
assert '20GB' in readme, 'Missing 20GB disk requirement'
assert 'docker compose up' in readme, 'Missing docker compose up command'
assert 'localhost:8080' in readme, 'Missing localhost:8080 URL'

# Check configuration docs
assert 'SURVIVALRAG_MODEL' in readme, 'Missing SURVIVALRAG_MODEL documentation'
assert 'OLLAMA_HOST' in readme, 'Missing OLLAMA_HOST documentation'
assert '.env.example' in readme, 'Missing .env.example reference'
assert 'external' in readme.lower() or 'External' in readme, 'Missing external Ollama documentation'

# Check GPU docs
assert 'GPU' in readme, 'Missing GPU section'
assert 'NVIDIA' in readme or 'nvidia' in readme, 'Missing NVIDIA GPU documentation'
assert 'macOS' in readme or 'macos' in readme or 'Mac' in readme, 'Missing macOS GPU limitation'
assert 'docker-compose.gpu.yml' in readme, 'Missing GPU compose file reference'

# Check CLI access docs
assert 'docker exec' in readme, 'Missing docker exec CLI access'
assert 'cli.py' in readme, 'Missing cli.py reference'

# Check troubleshooting
assert 'Troubleshoot' in readme or 'troubleshoot' in readme, 'Missing troubleshooting section'

# Check multi-arch build docs
assert 'buildx' in readme, 'Missing docker buildx multi-arch build documentation'
assert 'amd64' in readme.lower() or 'AMD64' in readme, 'Missing AMD64 architecture reference'
assert 'arm64' in readme.lower() or 'ARM64' in readme, 'Missing ARM64 architecture reference'

print('PASS: README.md has Quick Start, Hardware Requirements, Configuration, GPU, CLI, and Troubleshooting sections')
"
    </automated>
  </verify>
  <done>README.md updated with comprehensive deployment documentation: Quick Start guide (prerequisites + launch command), Hardware Requirements table (16GB RAM, 20GB disk, image sizes), Configuration section (env vars, external Ollama, model swapping with .env.example reference), GPU Acceleration (Linux NVIDIA + macOS Apple Silicon workaround), CLI Access (docker exec examples), and Troubleshooting (common problems + useful commands). All existing README content preserved.</done>
</task>

</tasks>

<verification>
- [ ] README.md is valid markdown
- [ ] Quick Start section documents `docker compose up` as the single startup command
- [ ] Hardware Requirements documents 16GB RAM and 20GB disk minimums
- [ ] Image sizes documented (~1GB app, ~7GB ollama)
- [ ] Configuration section references .env.example and documents all env vars
- [ ] External Ollama usage documented with OLLAMA_HOST
- [ ] GPU section covers both Linux NVIDIA (docker-compose.gpu.yml) and macOS limitation
- [ ] CLI access documented with docker exec examples
- [ ] Troubleshooting section covers common issues
- [ ] Multi-arch build instructions document docker buildx for AMD64 + ARM64
- [ ] All existing README.md content preserved
</verification>

<success_criteria>
README.md contains comprehensive Docker deployment documentation. A user reading the README can go from zero to a running system by following the Quick Start section (prerequisites + `docker compose up`). Hardware requirements (16GB RAM, 20GB disk) are clearly documented per DEPL-08. Configuration options (SURVIVALRAG_MODEL, OLLAMA_HOST, and other env vars) are documented with a reference to .env.example. GPU acceleration is documented for Linux NVIDIA (with docker-compose.gpu.yml) and macOS Apple Silicon (native Ollama workaround). CLI access via docker exec is documented. Common troubleshooting steps are provided. All existing README content is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-docker-packaging-deployment/08-03-SUMMARY.md`
</output>
